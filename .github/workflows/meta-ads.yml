name: Meta Ads Scraping

on:
  workflow_dispatch:
    inputs:
      query:
        description: '검색어'
        required: true
        type: string
      request_id:
        description: '요청 ID'
        required: true
        type: string
      limit:
        description: '페이지당 조회 수'
        required: false
        default: '100'
        type: string
      country:
        description: '국가 코드 (기본: KR)'
        required: false
        default: 'KR'
        type: string

jobs:
  scrape-meta-ads:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Run Meta Ads API
      env:
        KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
        KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
        META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
      run: |
        echo "🔍 검색어: ${{ github.event.inputs.query }}"
        echo "📋 요청 ID: ${{ github.event.inputs.request_id }}"
        echo "🌍 국가: ${{ github.event.inputs.country }}"
        python meta_ads_scraper.py \
          --query "${{ github.event.inputs.query }}" \
          --request-id "${{ github.event.inputs.request_id }}" \
          --limit ${{ github.event.inputs.limit }} \
          --country "${{ github.event.inputs.country }}"

    - name: Upload results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: meta-ads-results-${{ github.event.inputs.request_id }}
        path: "*.json"
        retention-days: 1
